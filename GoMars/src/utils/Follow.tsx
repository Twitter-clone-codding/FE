interface FollowProps {
  imgUrl?: string;
  hashtag?: string;
  userName?: string;
}

const Follow: React.FC<FollowProps> = (props) => {
  const { imgUrl, hashtag, userName } = props;

  return <div></div>;
};

export default Follow;
